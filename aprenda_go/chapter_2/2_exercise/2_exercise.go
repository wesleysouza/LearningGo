package main

import (
	"fmt"
)

var x int
var y string
var z bool

func main() {
	fmt.Println(x, y, z)
	//zero values -> values defined by the compiler when in one declaration values they is not defined
}
